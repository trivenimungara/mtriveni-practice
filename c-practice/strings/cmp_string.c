#include<stdio.h>
#include<string.h>
void main()
{
	char str1[10];
	char str2[20];
	printf("enter the strings:");
	scanf("%s %s",str1,str2);
	if(strcmp==0)
	{
		printf("strings are equal\n");
	}
	else
	{
		printf("strings are not equal\n");
	}
}
